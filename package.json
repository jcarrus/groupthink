{
  "name": "groupthink",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy && bun run scripts/register-commands.ts --all-guilds",
    "register": "bun run scripts/register-commands.ts",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260131.0",
    "@types/bun": "latest",
    "prettier": "^3.8.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "discord-interactions": "^4.4.0",
    "tweetnacl": "^1.0.3",
    "wrangler": "^4.61.1"
  }
}
